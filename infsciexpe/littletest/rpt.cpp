#include <iostream>
#include <stdio.h>
using namespace std;
#define repeat(x) for (int rijk = (x); rijk > 0; --rijk)
int main()
{
    int n;
    scanf("%d", &n);
    repeat(n)
    {
        // int
    }
}