#include <iostream>
#include <algorithm>

using namespace std;

int data[10005];

signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int n, m, k;
    cin >> n >> m >> k;
    for (int i = 1; i <= n; ++i)
    {
    }
    return 0;
}